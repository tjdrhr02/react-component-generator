{
  "permissions": {
    "allow": [
      "PowerShell(Get-ChildItem -Path . -Force | Select-Object Name, @{Name='Type';Expression={if\\($_.PSIsContainer\\){'폴더'}else{'파일'}}} | Format-Table -AutoSize)",
      "PowerShell(tree *)",
      "Bash(curl -s http://localhost:5173)",
      "PowerShell(git *)"
    ]
  },
  "enabledPlugins": {
    "frontend-design@claude-plugins-official": true
  }
}
